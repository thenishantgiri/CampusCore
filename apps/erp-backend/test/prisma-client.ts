export class PrismaClient {
  // you can add `user = { findUnique: () => {}, create: () => {} }`
  // if you ever need a default.
}

export enum RoleType {
  STATIC = 'STATIC',
  CUSTOM = 'CUSTOM',
}
